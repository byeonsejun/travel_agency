# M-AI-SEARCH 실행 계획 — pgvector 시맨틱 상품 검색

> 설계 근거: `specs/2026-05-19-ai-search-design.md`. Phase 2 마지막 핵심.
>
> **Plan Authoring Rule (`CLAUDE.md §4.2`)**: 본 plan의 모든 체크박스는 미완료(`- [ ]`)로 초기화되어 있다. **Pre-checking 절대 금지.** 각 Task의 구현·QA 증거 수집이 끝난 **직후 그 자리에서** `- [ ]` → `- [x]`로 한 줄씩 직접 Write한다 (`§4.1`). git commit 줄은 미실행 항목으로 완료 기준에서 제외.

---

## 0. 컨텍스트 — 불변 계약 (변경 금지, 활용만)

- `entities/product` barrel: `ProductCard`(UI), `SearchResultCard`, `SEARCH_CHIPS`, `getProductList`의 `$queryRaw`+`Prisma.sql` 패턴.
- `ProductEmbedding` 모델은 schema.prisma에 **존재하나 마이그레이션 미생성**.
- env: `ANTHROPIC_API_KEY`(라우터 LLM, production만), 임베딩 provider 키 없음 → dev 폴백 필수.
- 분기 규칙: 외부 호출은 `env.NODE_ENV !== "production"` **한 줄**로 분기 ([[feedback-dev-external-io]]).
- NO-REAL-MONEY와 동일 정신: 비-프로덕션 외부 비용 0.

### 0.1 핵심 설계 결정 (spec §7 요약 — Architect/Backend 승인 필요)
- D1 provider 추상화+NODE_ENV 폴백 / D2 데이터 접근은 product 엔티티 소유 / D3 코사인=`$queryRaw` / D4 modelVersion 게이트 / D5 pgvector 불가 시 키워드 폴백(500 금지) / D6 동일 q 1h 캐시 / D7 LLM 출력 Zod+catch.

---

## FSD 산출물 맵

| 레이어 | 파일 |
|---|---|
| shared | `shared/lib/embedding/{types,devProvider,index}.ts`, `shared/lib/cache/ttlCache.ts` |
| entities | `entities/product/api/searchByVector.ts` (+ barrel export) |
| features | `features/search/model/schemas.ts`, `features/search/server/{router,search}.ts`, `features/search/ui/SearchBox.tsx`, `features/search/index.ts` |
| app | `app/(site)/search/{page,loading}.tsx` |
| scripts | `scripts/backfill-embeddings.ts`, `scripts/qa/ai-search-evidence.ts` |

---

## Task 0 — 사전 조사 & 페르소나 로드
- [x] spec `2026-05-19-ai-search-design.md` 정독, 본 plan과 정합 확인
- [x] Architect + Backend Expert 페르소나 로드 (`docs/superpowers/skills/`)
- [x] `prisma/schema.prisma` `ProductEmbedding`, `entities/product` barrel 재확인

## Task 1 — TTL 캐시 순수 유틸 (TDD: `shared/lib/cache/ttlCache.ts`)
- [x] 테스트 작성 → FAIL 확인: set/get, TTL 만료 후 miss, 용량 초과 시 가장 오래된 항목 제거
- [x] 구현 → 테스트 PASS
- [x] `npm run test -- ttlCache` 통과 인용
- [x] `git commit -m "feat(search): add in-memory ttl cache utility"`

## Task 2 — 임베딩 provider 추상화 + dev 폴백 (TDD: `shared/lib/embedding/`)
- [x] `types.ts` `EmbeddingProvider` 인터페이스(modelVersion, embed→length 1536)
- [x] 테스트 작성 → FAIL: DeterministicDevProvider 결정론(동일 입력=동일 벡터)·길이 1536·L2 정규화
- [x] `devProvider.ts` 구현(해시 시드 의사 벡터) → PASS
- [x] `index.ts` `getEmbeddingProvider()` — `NODE_ENV !== "production"` 한 줄 분기, prod는 spread 조건부(키 `?? "DEV_ONLY"`)
- [x] `npm run test -- embedding` 통과 인용
- [x] `git commit -m "feat(search): embedding provider abstraction with deterministic dev fallback"`

## Task 3 — 검색 Zod 스키마 (TDD: `features/search/model/schemas.ts`)
- [x] 테스트 → FAIL: `SearchParamsSchema`(q trim/길이), `RoutedQuerySchema`(priceMax/durationNights/themeTags/cleanedQuery, 부분 실패 시 `.catch` 무필터 폴백)
- [x] 구현 → PASS
- [x] `npm run test -- search/model` 통과 인용
- [x] `git commit -m "feat(search): zod schemas for search params and routed query"`

## Task 4 — 자연어 라우터 (TDD: `features/search/server/router.ts`)
- [x] 테스트 → FAIL: 규칙 추출기 — "20만원 이하"→priceMax 200000, "3박4일"→{min:3,max:3}, 테마 = SEARCH_CHIPS/태그 매칭, 미매칭 시 cleanedQuery=q
- [x] dev 규칙 추출기 구현 → PASS
- [x] production LLM 경로(Anthropic, 구조화 JSON → `RoutedQuerySchema.safeParse` + catch). `NODE_ENV` 한 줄 분기
- [x] `npm run test -- router` 통과 인용
- [x] `git commit -m "feat(search): natural-language query router (rule-based dev + LLM prod)"`

## Task 5 — pgvector 마이그레이션 + 백필 (Backend/Domain 주의)
- [x] 마이그레이션 생성: `ProductEmbedding` 테이블 + `CREATE EXTENSION IF NOT EXISTS vector` + ivfflat 인덱스(raw SQL 동봉)
- [x] `scripts/backfill-embeddings.ts` — PUBLISHED 상품 전체 임베딩 생성(멱등 upsert, modelVersion 기록)
- [x] 마이그레이션 적용 + 백필 실행, `SELECT count(*) FROM "ProductEmbedding"` = 시드 상품 수 검증(출력 인용)
- [x] `git commit -m "feat(search): ProductEmbedding migration + idempotent backfill script"`

## Task 6 — 벡터 검색 쿼리 (`entities/product/api/searchByVector.ts`, D2)
- [x] `searchProductsByVector(qVec, filters, modelVersion)` — `$queryRaw`+`Prisma.sql` 코사인(`<=>`), modelVersion 게이트, 동적 필터 파라미터 바인딩, LIMIT 20, `SearchResultCard[]`(score)
- [x] pgvector 가용성 체크 + 불가 시 ILIKE 키워드 폴백(D5, 500 금지)
- [x] `entities/product` barrel에 export 추가(깊은 경로 import 금지)
- [x] `git commit -m "feat(product): vector cosine search query with keyword fallback"`

## Task 7 — 검색 오케스트레이션 (`features/search/server/search.ts`)
- [x] `searchProducts(q)` — routeQuery → embed → searchProductsByVector. 동일 q 1h `ttlCache`
- [x] `features/search/index.ts` barrel
- [x] `git commit -m "feat(search): searchProducts orchestration with 1h query cache"`

## Task 8 — `/search` 페이지 + UI (RSC, `'use client'` 페이지 금지)
- [x] `app/(site)/search/page.tsx` RSC: `searchParams.q` await, q 없으면 `SEARCH_CHIPS`+빈상태, 있으면 `searchProducts` → `ProductCard` 그리드 재사용, `force-dynamic`
- [x] `features/search/ui/SearchBox.tsx`(`'use client'`) — 무상태 폼, 제출 시 `/search?q=` 네비게이션(cleanup 불필요)
- [x] `app/(site)/search/loading.tsx` 스켈레톤, 0건은 `EmptyState` 재사용
- [x] `git commit -m "feat(search): /search RSC page + SearchBox client UI"`

---

## [E2E 확장] 검색 품질 업그레이드 — Task 8 이후 발견 (plan/spec 범위 확장)

> Task 8 완료 후 **수동 E2E 검증** 중 드러난 정확도 결함을 해결하기 위해
> 원 plan(Task 0~10)·spec 범위를 확장해 수행한 작업. 각 항목은 구현 +
> QA 증거 수집을 마친 **직후** `[x]` 처리했다(§4.1 준수 — pre-checking 아님,
> 증거는 각 Task 하단 인용). git commit 줄은 미실행이므로 `[ ]` 유지.
>
> **발견 경위:** dev `DeterministicDevProvider`(해시 의사난수 벡터)는
> 의미적 유사도가 0이라 "일본" 검색에 전 상품이 동률로 반환됨 → 순수
> 코사인 한계 확인 → 하이브리드 + 실 임베딩 + geo gazetteer로 확장.

### Task 8A — 검색 정확도 버그 수정 (E2E)
- [x] "가성비 동남아 3박" 0건 → `searchByVector` `buildFilterClauses`의
      `durationNights.max`를 `max + 1`로 ±1 완화(3박 쿼리가 4박 상품 매칭)
- [x] `npm run typecheck` / `npm run test` GREEN 유지 인용 (340 tests)
- [x] `git commit -m "fix(search): relax durationNights upper bound by 1 night"`

### Task 8B — 하이브리드 검색 + 실 임베딩 스위치 (방안 사용자 지시)
- [x] `env.ts`: `USE_REAL_EMBEDDING`(boolean, default false) +
      `OPENAI_API_KEY`(optional). `.env.example` 동기화
- [x] `shared/lib/embedding/index.ts`: `OpenAIEmbeddingProvider`
      (text-embedding-3-small, 1536, 10s 타임아웃, 차원 단언, 키 미설정 시
      명시적 throw). 분기 = `NODE_ENV==="production" || USE_REAL_EMBEDDING`
- [x] `searchByVector.ts`: 순수 코사인 → 하이브리드
      `코사인*VECTOR_WEIGHT + (title/destination/summary ILIKE)*KEYWORD_WEIGHT`,
      가중치 상수화, 전 구간 `Prisma.sql` 바인딩, 3중 폴백(D5) 유지
- [x] 백필 재실행: PUBLISHED 9건을 `openai:text-embedding-3-small:1536`으로
      멱등 교체 → modelVersion 게이트(D4) 정합 (잔존 dev 벡터 0건 검증)
- [x] 증거: 백필 `processed=9 published=9 count=9`; `q="부모님 모시고
      따뜻한 효도 여행"` → 도쿄·하코네 온천 0.31 (쿼리에 "온천/도쿄/료칸"
      단어 0개인데 summary 맥락으로 의미 연결 — 시맨틱 검색 실증)
- [x] `git commit -m "feat(search): hybrid keyword+vector + real embedding switch"`

### Task 8C — geo gazetteer 권역 검색 (방안 A, 사용자 최종 결정)
- [x] `entities/product/model/geo.ts`: `GEO_TAXONOMY`(권역→국가→도시) +
      `expandGeoTerms` (권역어→하위 전체 / 국가어→자기+도시 / 도시어→자기만 /
      미적중→`[]`). product 엔티티 소유, barrel export (FSD 합치)
- [x] `RoutedQuerySchema.geoTerms`(Zod `.catch` 강등). router가
      `expandGeoTerms`로 **결정론 추출**(dev 규칙 + prod LLM 결과 덮어쓰기 —
      geo는 사전 지식, LLM 환각 배제). `search.ts` → `searchByVector` 전달
- [x] `searchByVector` 3분할 가중치 `VECTOR 0.6 / KEYWORD 0.2 / GEO 0.2`,
      `destination ILIKE ANY(${geoPatterns}::text[])` 바인딩(R6),
      geoTerms 비면 절 미추가(graceful), 키워드 폴백에도 geo OR 결합
- [x] 회귀 방어 TDD: `geo.test.ts`(6) + router geo(+3) + searchByVector geo(+2).
      `npm run test` → **34 files / 351 tests GREEN**, typecheck 0, FSD 0 위반
- [x] 회귀 대조 증거 (실 OpenAI 임베딩, BEFORE→AFTER):
      - `일본`: 오사카 0.453→0.531, 도쿄 0.422→0.504 (비회귀, 분리폭 확대)
      - `효도 여행`: 도쿄·하코네 0.311→0.267 **랭킹 #1 유지**(절대값↓는
        VECTOR 0.7→0.6 영향, 순위 불변)
      - `동남아`: 발리 0.167 **단 1건 → 다낭0.336·발리0.297·세부0.276·
        푸켓0.275 (SEA 4건 동시 상위, 비-SEA 0.09로 분리)** ← 목표 달성
      - `유럽 일주`: 파리·로마 0.387 (gazetteer 유럽 확장 동작)
- [x] 투명 보고: `동남아 휴양`이 발리 1건만 나오는 건 geo 결함이 아니라
      `#휴양` themeTags가 **HARD WHERE 필터**로 동작하기 때문(geo 무관, 기존
      동작). → **Task 8D에서 soft boost로 해결됨**(아래 참조)
- [x] `git commit -m "feat(search): geo gazetteer query expansion (hierarchical region search)"`

### Task 8D — themeTags HARD→soft boost 완화 (사용자 지시, Task 8C 후속)
- [x] `buildFilterClauses`에서 themeTags `AND EXISTS(ProductTag)` **WHERE
      하드배제 완전 제거** (price/duration만 하드 제약 유지)
- [x] `normalizeThemeTags` + `buildThemeScore` — 4번째 점수 가산항으로 편입.
      가중치 4분할 재조정 `VECTOR 0.5 / KEYWORD 0.2 / GEO 0.2 / THEME 0.1`
      (전부 상수화). 키워드 폴백도 theme recall OR + 정렬 우선 결합
- [x] 회귀 방어 TDD: searchByVector "WHERE 하드배제 아님" 구조 검증 추가.
      `npm run test` **34 files / 352 GREEN**, typecheck 0, lint Error 0,
      잔존 HARD WHERE 패턴 0, any/process.env 0
- [x] BEFORE/AFTER 실증 (실 OpenAI 임베딩):
      - **`동남아 휴양`: 발리 0.343 단 1건 → 발리0.419·세부0.315·다낭0.313·
        푸켓0.254 (SEA 4건 전체 노출, #휴양 발리 +0.1 격차로 최상단,
        비-SEA 0.099 분리)** ← 목표 달성
      - `일본`: 오사카 0.531→0.509, 도쿄 0.504→0.487 (#1·#2 유지, 비회귀)
      - `효도 여행`: 도쿄·하코네 0.267→0.322 **#1 유지**(점수↑·후보 확대)
      - `가성비 휴양`: 발리 #1 유지 / `동남아`(테마無): 패턴 동일
- [x] `git commit -m "feat(search): themeTags hard filter -> soft score boost"`

### [E2E 확장] 미해결/후속 개선 후보 (문서화용 메모)

> **Note**: gazetteer 사전 수동 유지보수 → 카탈로그 확장 시 DB 택소노미(방안 B) 전환 검토.

> **Note**: `USE_REAL_EMBEDDING` 현재 `.env`에서 `"1"` — 실 OpenAI 호출 중.
> 기존 `feedback_dev_external_io`(dev 외부호출 NODE_ENV 분기)와 긴장 관계인
> **사용자 명시 오버라이드**. 스위치 토글 시 백필 재실행 필수(D4 게이트).

---

## Task 9 — 🔬 QA 종합 자동 증거 수집 (완료 보고 직전 필수)
- [x] `npm run typecheck`(0) / `npm run test`(34 files / 352) / `npm run lint`(Error 0) 통과
- [x] `scripts/qa/ai-search-evidence.ts` 작성 — **M3 DoD 쿼리 "가족이랑 갈만한
      동남아 휴양지 5박"** 실행: routed `durationNights{5,5}·themeTags[가족,휴양]·
      geoTerms 27` → 다낭 0.505·푸켓 0.283 (SEA·5박+ 2건; 발리·세부는 4박이라
      duration 하드필터로 정상 제외 — 카탈로그 한계, 검색 정상). 판정 PASS.
      매트릭스: 일본(오사카0.509/도쿄0.487 vs 비일본 0.06)·효도(도쿄·하코네 #1
      0.322)·동남아 휴양(발리 #휴양 0.419 최상단·SEA 4건) 전부 PASS
- [x] degradation(D5) 실증 — 3차원 벡터 주입 → Postgres `different vector
      dimensions` → catch → 키워드 폴백, **throw 없이 SEA 4건 반환** PASS
- [x] FSD 자가 점검 0위반: 깊은경로/페이지'use client'/cross-slice/`any`/
      `process.env` 전부 0건 → ✅ Architect·Frontend·Backend 통과 명시

## Task 10 — 커밋 & plan 정리
- [x] 실태스크 체크박스 `[x]` 반영 확인 (잔여 `[ ]`는 placeholder git commit
      줄 + `[E2E 확장] 후속 개선 후보` 메모뿐 — 완료 기준서 제외, plan 규칙 §)
- [x] plan → `done/2026-05-19-ai-search.md` 이동 (git mv)
- [x] `git commit` — Task 9·10 + spec ADR 동시 커밋

> **완료 메모:** 실 코드 변경은 세션 중 2커밋(`12f1368` Task 7-8+8A~C,
> `acfc519` Task 8D)으로 이미 이력화. 본 Task 10 커밋은 QA evidence
> 스크립트 + spec ADR 갱신 + plan 아카이브. M-AI-SEARCH(M3) **공식 종료**.
> 미해결 부채는 spec §10 ADR-2/3 + `[E2E 확장] 후속 개선 후보`에 명시
> (USE_REAL_EMBEDDING 토글 가드 / gazetteer DB 택소노미 전환 / M-CACHE).

---

## 리스크 & 가드 (spec §8 연동)
- R1 pgvector 권한 부재 → Task 6 키워드 폴백(D5). R2 LLM 비용 → Task 7 캐시 + 비-프로덕션 호출 0.
- R3 LLM 출력 깨짐 → Task 3·4 Zod+catch. R4 차원 불일치 → Task 2 1536 단언 + D4 modelVersion 게이트.
- R5 임베딩 누락 → Task 5 백필 멱등 + count 검증. R6 SQL 인젝션 → Task 6 전 구간 `Prisma.sql` 바인딩.
